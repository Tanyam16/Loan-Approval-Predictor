services:
  - type: web
    name: loan-approval-predictor
    env: python
    runtime: python-3.11.0
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app_v2:app --timeout 120
    plan: free