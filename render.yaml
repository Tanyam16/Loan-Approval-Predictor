services:
  - type: web
    name: loan-approval-predictor
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app_v2:app --timeout 120 --bind 0.0.0.0:$PORT
    plan: free